// Package main provides the standort server command-line entrypoint.
//
// The executable registers the `server` command, which starts the service using
// the dependency-injection graph composed under `internal/cmd`.
package main
